# Contributing

Fork the repo, create a branch, and open a PR. Run `npm run lint` and `npm run typecheck` before submitting.

For bugs, open an issue with steps to reproduce. For features, open an issue describing the use case first.
